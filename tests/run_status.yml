description: Test to validate workflow run status
service: WES
versions:
  - 1.0.0
tags:
  - schema_validation_only
jobs:
  - $ref: "templates/run_workflow_template.yml"
  - name: run_status
    description: Retrieve the workflow run status
    endpoint: /runs/{run_id}/status
    operation: GET
    path_parameters:
      run_id: "{run_id}"
    response:
      200: {}